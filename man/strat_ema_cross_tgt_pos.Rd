% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strat_ema_cross.R
\name{strat_ema_cross_tgt_pos}
\alias{strat_ema_cross_tgt_pos}
\title{EMA-Cross Target Positions}
\usage{
strat_ema_cross_tgt_pos(
  DT,
  fast = 20L,
  slow = 50L,
  low_atr_threshold = 5L,
  freshness_floor = 18L,
  tp_ratio = 0.05,
  sl_ratio = 0.02,
  compute_features = TRUE,
  atr_h = 12L,
  atr_window = 300L,
  debug = FALSE
)
}
\arguments{
\item{DT}{A candle `data.table`.}

\item{fast}{Integer fast EMA length.}

\item{slow}{Integer slow EMA length.}

\item{low_atr_threshold}{Numeric percentile threshold used to define the
low-volatility gate.}

\item{freshness_floor}{Integer maximum cross age allowed for a live target.}

\item{tp_ratio}{Numeric take-profit guard used by `calc_EMA_cross()`.}

\item{sl_ratio}{Numeric stop-loss guard used by `calc_EMA_cross()`.}

\item{compute_features}{Logical; when `TRUE`, missing EMA and ATR features are
added to `DT` in place.}

\item{atr_h}{Numeric ATR half-life used by the low-volatility gate.}

\item{atr_window}{Integer rolling window used by the ATR quantile.}

\item{debug}{Logical; when `TRUE`, returns a list with the generated target
column name.}
}
\value{
A numeric vector of target positions, or a list when `debug = TRUE`.
}
\description{
Generates target positions from a simple EMA-cross strategy with ATR-based
gating. The wrapper can either reuse precomputed features or compute the
required EMA and ATR features in place.
}
