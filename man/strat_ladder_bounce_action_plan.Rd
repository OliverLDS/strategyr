% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strat_ladder_bounce.R
\name{strat_ladder_bounce_action_plan}
\alias{strat_ladder_bounce_action_plan}
\title{Ladder Bounce Action Plan}
\usage{
strat_ladder_bounce_action_plan(
  DT,
  state,
  cycle_N = 180L,
  span = 3L,
  refined = TRUE,
  min_swing = 0.05,
  center_idx = 9L,
  lower = 7L,
  upper = 13L,
  target_size = 1,
  compute_ladder = TRUE,
  detailed_report = FALSE,
  strat_id = 201L,
  tol_pos = 0.1,
  debug = FALSE
)
}
\arguments{
\item{DT}{A candle `data.table`. When `compute_ladder = TRUE`, it must contain
`datetime`, `high`, `low`, and `close`.}

\item{state}{Named list describing the current trading state.}

\item{cycle_N}{Integer cycle width passed to `calc_ladder_index()`.}

\item{span}{Integer pivot span passed to `calc_ladder_index()`.}

\item{refined}{Logical pivot refinement flag.}

\item{min_swing}{Minimum swing threshold passed to `calc_ladder_index()`.}

\item{center_idx}{Zero-based native center index for ladder mapping.}

\item{lower}{Integer lower ladder threshold.}

\item{upper}{Integer upper ladder threshold.}

\item{target_size}{Numeric absolute target exposure.}

\item{compute_ladder}{Logical; compute the ladder index when missing.}

\item{detailed_report}{Logical; forwarded to `calc_ladder_index()`.}

\item{strat_id}{Integer strategy identifier recorded on generated actions.}

\item{tol_pos}{Numeric tolerance passed to the action planner.}

\item{debug}{Logical; when `TRUE`, returns a list with the generated target
column name.}
}
\value{
A list produced by `gen_action_plan_rcpp()`.
}
\description{
Applies the naive ladder-bounce rule to the latest bar and translates the
resulting target exposure into an executable action plan.
}
