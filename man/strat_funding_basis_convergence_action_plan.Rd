% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strat_funding_basis_convergence.R
\name{strat_funding_basis_convergence_action_plan}
\alias{strat_funding_basis_convergence_action_plan}
\title{Funding-Basis-Convergence Action Plan}
\usage{
strat_funding_basis_convergence_action_plan(
  DT,
  state,
  z_n = 20L,
  entry_z = 2,
  exit_z = 0.5,
  basis_col = NULL,
  spot_col = "spot",
  forward_col = "forward",
  domestic_rate_col = "r_domestic",
  foreign_rate_col = "r_foreign",
  tenor_col = "tenor_years",
  tenor_tag = "1m",
  funding_col = NULL,
  funding_weight = 1,
  target_size = 1,
  compute_features = TRUE,
  sample = TRUE,
  strat_id = 611L,
  tol_pos = 0.1,
  debug = FALSE
)
}
\arguments{
\item{DT}{A `data.table` containing a precomputed basis column or the inputs
needed to build an FX-style basis.}

\item{state}{Named list describing the current trading state.}

\item{z_n}{Integer rolling window used for z-scores.}

\item{entry_z}{Numeric absolute z-score threshold used for entries.}

\item{exit_z}{Numeric absolute z-score threshold used for exits.}

\item{basis_col}{Optional basis column name. Defaults to `fx_basis_<tag>`.}

\item{spot_col}{Spot-price column name used when basis must be built.}

\item{forward_col}{Forward-price column name used when basis must be built.}

\item{domestic_rate_col}{Domestic-rate column used when basis must be built.}

\item{foreign_rate_col}{Foreign-rate column used when basis must be built.}

\item{tenor_col}{Tenor-in-years column used when basis must be built.}

\item{tenor_tag}{Character label appended to the built basis column name.}

\item{funding_col}{Optional funding-rate column added into the signal.}

\item{funding_weight}{Numeric scaling applied to `funding_col`.}

\item{target_size}{Numeric absolute target exposure.}

\item{compute_features}{Logical; when `TRUE`, missing basis, signal, and
z-score features are added to `DT` in place.}

\item{sample}{Logical; if `TRUE`, rolling z-score uses sample standard
deviation.}

\item{strat_id}{Integer strategy identifier recorded on generated actions.}

\item{tol_pos}{Numeric tolerance passed to the action planner.}

\item{debug}{Logical; when `TRUE`, returns a list with the generated target
vector and feature column names.}
}
\value{
A list produced by `gen_action_plan_rcpp()`.
}
\description{
Applies the funding-and-basis convergence rule to the latest row and
translates the resulting target exposure into an executable action plan.
}
