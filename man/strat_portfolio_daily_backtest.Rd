% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strat_portfolio_daily_backtest.R
\name{strat_portfolio_daily_backtest}
\alias{strat_portfolio_daily_backtest}
\title{Backtest Daily Portfolio Target Weights}
\usage{
strat_portfolio_daily_backtest(
  ohlc,
  target_weights,
  initial_cash = 1e+06,
  fee_rt = 5e-04,
  rebalance_tolerance = 0,
  date_col = "date",
  asset_col = "asset",
  open_col = "open",
  high_col = "high",
  low_col = "low",
  close_col = "close",
  target_weight_col = "target_weight",
  rebalance_col = "rebalance_due"
)
}
\arguments{
\item{ohlc}{Long daily OHLC \code{data.table} with one row per available \code{date}-\code{asset} pair.}

\item{target_weights}{Long target-weight \code{data.table} with exactly one row for every available \code{date}-\code{asset} pair in \code{ohlc}.}

\item{initial_cash}{Initial portfolio cash.}

\item{fee_rt}{Proportional fee charged on traded notional.}

\item{rebalance_tolerance}{Absolute weight difference below which an otherwise eligible rebalance is skipped.}

\item{date_col, asset_col}{Date and asset identifier column names.}

\item{open_col, high_col, low_col, close_col}{Daily OHLC column names.}

\item{target_weight_col}{Target-weight column name.}

\item{rebalance_col}{Optional logical eligibility column in \code{target_weights}. When it exists, only \code{TRUE} rows may rebalance; when it is absent or \code{NULL}, every available target row is eligible.}
}
\value{
A named list of stable \code{data.table}s: \code{equity} contains daily equity,
return, cash, exposure, turnover, fees, and availability counts; \code{weights}
contains daily target and realized weights with explicit availability states;
and \code{rebalances} contains the daily rebalance and cost summary.
}
\description{
Executes a long-only daily target-weight panel with an explicit cash residual.
A target row is executed at that row's open; target generators such as
\code{strat_equal_weight_rebalance_target_weights()} already shift
completed-bar signals to that next eligible open. Missing market rows are
reported as unavailable, are never traded, and retain their last close only as
an explicitly flagged stale valuation.
}
