% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strategy_mining.R
\name{mine_strategy_walk_forward}
\alias{mine_strategy_walk_forward}
\title{Walk-Forward Strategy Mining}
\usage{
mine_strategy_walk_forward(
  DT,
  strategy_fun,
  param_grid,
  train_years = 3,
  test_years = 1,
  step_years = 1,
  n_best = 1L,
  from = NULL,
  to = NULL,
  min_train_rows = 200L,
  min_test_rows = 50L,
  warmup_days = 365L,
  warmup_years = NULL,
  score_col = "sortino",
  keep_paths = FALSE,
  strat_id = 0L,
  asset_id = 0L,
  ctr_size = 1,
  ctr_step = 1,
  lev = 10,
  fee_rt = 0,
  fund_rt = 0,
  tol_pos = 0.1,
  rec = FALSE,
  annualization = 252,
  risk_free_return = 0,
  min_acceptable_return = 0,
  turnover_tol = 0
)
}
\arguments{
\item{DT}{Candle `data.table` containing `datetime`, `open`, `high`, `low`,
and `close`.}

\item{strategy_fun}{Strategy target-position function. It must accept `DT` as
its first argument and return a numeric target-position vector.}

\item{param_grid}{Non-empty list, `data.frame`, or `data.table` of parameter
values.}

\item{train_years, test_years}{Numeric train and test window lengths in
calendar years.}

\item{step_years}{Numeric step between successive train windows in calendar
years.}

\item{n_best}{Integer number of top training parameter rows tested in each
out-of-sample window.}

\item{from, to}{Optional date filters for the fixed backtesting period.}

\item{min_train_rows, min_test_rows}{Minimum OHLC rows required for each train
or test slice.}

\item{warmup_days, warmup_years}{Warmup history used for test signal
construction. `warmup_years`, when supplied, overrides `warmup_days`.}

\item{score_col}{Metric used for descending ranking.}

\item{keep_paths}{Logical; when `TRUE`, include list-columns with target
positions and equity curves.}

\item{strat_id}{Integer strategy identifier passed to `backtest_rcpp()`.}

\item{asset_id}{Integer asset identifier passed to `backtest_rcpp()`.}

\item{ctr_size, ctr_step, lev, fee_rt, fund_rt, tol_pos}{Backtest execution
assumptions passed to `backtest_rcpp()`.}

\item{rec}{Logical; when `TRUE`, the backtest engine records execution traces
on the equity attribute. Keep this `FALSE` for larger mining jobs.}

\item{annualization, risk_free_return, min_acceptable_return}{Performance
metric assumptions passed to `calc_backtest_performance()`.}

\item{turnover_tol}{Numeric tolerance below which target-position changes are
ignored for turnover and trade counts.}
}
\value{
A list with `windows`, `train_results`, and ranked `test_results`
  `data.table`s.
}
\description{
Performs rolling train/test strategy mining. For each window, parameter rows
are ranked on the training slice, the best rows are evaluated on the
following test slice, and test signals are computed on a wider window that can
include warmup history.
}
